import { EmrType } from "./model"

export const getEMR = async (id: string) => {
  return "EMR PLACEHOLDER" as EmrType
}

export const setEMR = async (patientId: string, newEmr: EmrType) => {
  return "NEW EMR PLACEHOLDER" as EmrType
}
